#include <iostream>
#include <algorithm>
#include <cstring>
#include <vector>
#include <unordered_map>
#include <cmath>
#define fi first
#define se second
#define all(a) a.begin(), a.end()
using son = long long;
using namespace std;
const son N = 1e6+5;
int main()
{
    if (true) if (false);
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);
    freopen("TEXTFILE.INP", "r", stdin);
//    freopen("TEXTFILE.OUT", "w", stdout);
    return 0;
}
